
enum Role {
   CUSTOMER
   PROVIDER
   ADMIN 
}
model User{
    id String @id @default(uuid())
    name String
    email String @unique
    password String
    role Role @default(CUSTOMER)

    equipments Equipments[]
   bookings Booking[]
}